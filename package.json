{
  "name": "skyroute-enterprise",
  "private": true,
  "workspaces": [
    "astro",
    "worker"
  ],
  "scripts": {
    "build": "npm run build --workspace astro",
    "dev": "npm run dev --workspace astro"
  }
}
