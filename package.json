{
  "name": "skyroute-enterprise-root",
  "version": "1.0.0",
  "description": "Root package for SkyRoute-Enterprise",
  "main": "index.js", // Or your actual main file for the worker
  "scripts": {
    "test": "jest" // Assuming you have a test script for the worker
    // Add other scripts if needed, e.g., for deploying the worker
  },
  "dependencies": {
    "worker": "^0.4.0"
  },
  "devDependencies": {
    "jest": "^29.0.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
